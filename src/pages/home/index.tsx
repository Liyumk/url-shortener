import { Box, Heading } from "@chakra-ui/react";
import React from "react";

const Home = () => {
    return (
        <Box>
            <Heading as="h2">Home of url shortener</Heading>
        </Box>
    );
};

export default Home;
